# Testing Guide — /Users/hendrikk/Developer/ria/service-module-testbed

> Generated by risttee on 2026-07-09T09:48:19.746255Z
> Profile schema: 1.0

## Quick Start

1. Copy `.env.example` to `.env` and fill in values
2. Start the service:

```bash
   docker compose -f docker-compose.yml up -d
```

3. Install Playwright dependencies:
```bash
   npm ci
   npx playwright install --with-deps
```
4. Run smoke tests:
```bash
   npx playwright test
```

## Stack

| Field | Value |
|-------|-------|
| Stack | Node / npm |
| Port | 8086 |
| Health endpoint | none detected |

## Smoke Test Coverage

0 tests generated from
0 detected surfaces.

| Method | Path | Reason |
|--------|------|--------|


## Coverage Gaps

- **Guarded endpoints**: 0 surfaces have a `.guard`
  file — authentication setup is required before these tests can pass.
- **Untested surfaces**: 0
  surfaces were detected but not included in the smoke set.
  See `.kratest/ASSUMPTIONS.md` for suggested next targets.

## CI

A GitHub Actions workflow has been generated at
`.github/workflows/playwright.yml`.
It starts the service, waits for the health endpoint, and runs the
full smoke suite on every push and pull request.
